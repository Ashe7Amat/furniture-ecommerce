// Se carga automáticamente antes de cada archivo de test (ver vite.config.js).
// Añade los matchers de jest-dom (toBeInTheDocument, toHaveTextContent, etc.) a expect().
import '@testing-library/jest-dom';
